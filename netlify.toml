# Configuración de build para Netlify
[build]
  # Directorio base donde se encuentra el código del frontend
  base = "frontend"
  # Comando para construir el sitio
  command = "npm run build"
  # Directorio de publicación (relativo al directorio base)
  publish = "dist"

# Regla de redirección para Single Page Applications (SPA)
# Esto asegura que todas las rutas sean manejadas por index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

